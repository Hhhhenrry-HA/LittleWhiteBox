# 记忆 Agent 维护：验证记录

本文按实施轮次保留事实。**当前轻量化实现以第 10 节为准**；前面各节的旧工具协议、覆盖统计及测试计数只记录当时版本，不代表现行行为。

日期：2026-09-26。施工状态见[施工文档](./memory-maintenance-implementation-plan.md)，产品判据见[目标设计](./memory-maintenance-target-design.md)。

## 1. 本轮实现与使用入口

总结设置 → **Agent**。自动维护默认关闭；保存授权不会调用模型。手动「复审一次」独立于自动开关。后台由总结宿主拥有，关闭设置页不会停止任务。共享主预设不需要其他 APP 启动。

当前实现覆盖逐楼锚点及整份总结业务 JSON、跨批事件合并／引用重定向、只读分页证据、草稿提交、批次成果与撤销、受影响索引补齐。原文不写入；运行队列不持久化，不重启续发付费请求。

维护操作、撤销和成果共享同一差量；同批多次维护按顺序记录。宿主 `saveMetadata` 可能吞掉保存失败，因此增加聊天文件回查确认；只读回查可重试，未知保存不重跑语义模型。

## 2. 工程验证

| 实际执行 | 结果 |
| --- | --- |
| `node --test modules/story-summary/tests/*.test.js` | 最新结果见第 9 节；历史存储与 UI 回归仍保留 |
| `npm run test:story-summary:runtime` | 通过 |
| `npm run test:story-summary:alignment` | 通过，零网络对齐检查 |
| `npm run test:story-summary:cancel` | 通过：请求 12、响应 102、延迟 12、存储 7、swipe 6 个场景；另含维护存储检查 |
| `npm run test:story-summary:bundle` | 通过，受影响 replay 构建；未重建地图／OS |
| 生产 `story-summary.js` 入口 esbuild 链接检查 | 通过；宿主外部模块外置，`write:false`，不改发布产物 |
| `npm run test:story-summary:vectors` | 41/41 通过 |
| `npm run test:gold-eval` | 历史首轮 166/168 及定点恢复见第 6 节；本轮冻结工作区后的完整复跑记录见第 7 节，只跑离线测试 |
| `npm run lint`、`git diff --check` | 最新结果见第 9 节；不修范围外模块 |

主要证据入口：

- [领域、会话、保存边界测试](../tests/memory-maintenance.test.js)：事件身份和引用、独立事件不受操作波及、原文边界与不可写、已读证据、分页未读完不可编辑、覆盖缺口、别名环、预算终止、共享参数、工具错误回喂、聊天／来源／人工编辑冲突、取消和重启。
- [实际存储集成](../../../scripts/story-summary-replay/memory-maintenance-storage-check.mjs)：真实 store 与 IndexedDB 接口，模拟宿主保存。验证同批两次维护、导入基线维护撤销、旧事件／锚点向量失效、锚点删除后楼层统计、已确认保存失败恢复、原文不变。
- [总结提交通知集成](../../../scripts/story-summary-replay/summary-response-check.mjs)：实际生成器在保存成功后通知一次；解析／保存失败不通知；收尾回调失败仍通知；重复已提交边界零调用、零通知。手动与自动入口共用该生成器，不另造两条通知路径。
- [保存确认](../data/metadata-confirmation.js)：生产回查使用 SillyTavern 当前单聊／群聊读取 API。单测验证吞错、回查重试和未知结果；replay 使用隔离宿主替身，未向用户真实聊天写入测试数据。

操作测试证明“指令执行完整且可撤销”，**不证明模型知道哪条该改、哪两条该合并**。

## 3. 浏览器实测

入口：[真实产品 iframe 预览](../tests/ui-preview.mjs)；宿主数据来自[确定性桥接样本](../tests/maintenance-preview-host.js)。通过 Playwright CLI 驱动 Chromium，桌面 1280×800、手机视口 390×844；不是实体手机测试，也不是生产付费 Agent 端到端测试。

已检查：

- 空态、12 条分页成果、展开修改前后／理由／依据、未检查／无法查证／缺失锚点。
- 保存授权没有发出复审消息；关闭重开保留授权。
- 手动复审显示运行与取消；关闭设置后宿主运行仍在；取消可用。
- 深浅主题及长内容；手机内容无横向溢出。

截图位于 [`output/playwright/memory-maintenance/`](../../../output/playwright/memory-maintenance/)：`desktop-light.png`、`desktop-light-detail.png`、`desktop-dark.png`、`mobile-dark-detail.png`、`mobile-dark-error.png`、`mobile-light-empty.png`、`mobile-light-running.png`。

按前端设计技能沿用现有设置页、集中必要文案，没有新增 API 表单或工具日志控制台。按提示词技能将模型规则集中在维护领域，工具权限由代码验证。

## 4. 固定语义样本与真实调用结果

样本：[24 楼受控对话](../tests/fixtures/memory-maintenance.js)，不读取或改写用户原始聊天。运行器：[小样本检查](../tests/maintenance-model-check.mjs)，默认零 API 预检，显式 `--run` 才调用。

`--run` 产出：每次请求／响应原文、`turns.jsonl`（每轮模型调用的包号、耗时、用量，以及每个工具返回给模型的状态、错误码、被拒条目和 `notes`）、`summary.json`。`summary.json` 的协议门槛由 [验收报告](../tests/maintenance-acceptance.js) 计算：每包被拒轮次不超过 1（无工具调用、工具报错或提交返回 `needs_fix` 都算一次），平均每包不超过 2 轮；运行未完成或任一门槛未达即判失败、进程退出码为 1。下列冻结评价项在 `summary.json.cases` 中列出每条记录的最终状态与所受操作，是否正确由人工判断。

冻结评价项：

- 大总结：传闻确定化、药箱归属、长沙／北京历史与现状、客房小位置误作持续事实，共 4 个已知错误。
- L0：传闻确定化、归属颠倒、目的地错误，共 3 个已知错误。
- 正确对照：旧书承诺、餐厅当楼锚点、另一次青山争吵。
- 事件：红山争吵第 17–23 楼被第 20 楼边界切成两条；应保留旧身份、续接细节、重定向后续因果，保留青山独立事件。

| 质量项目 | 修正／正确合并 | 误改／误合并 | 未解决／漏合并 |
| --- | --- | --- | --- |
| 大总结 | 未测得 | 未测得 | 未测得 |
| L0 | 未测得 | 未测得 | 未测得 |
| 事件整理及细节保留 | 未测得 | 未测得 | 未测得 |

原因：现有共享配置各次尝试均在产出模型结果前失败。没有 `result.json`，不存在可评分语义输出。

| 留存目录（仓库根目录下） | 真实结果 |
| --- | --- |
| `output/memory-maintenance-check/` | 当前主预设凭据被拒绝；保留请求和 journal，早期脚本尚无独立 failure JSON |
| `output/memory-maintenance-check-smol/` | 已配置备选预设凭据被拒绝；保留失败回执 |
| `output/memory-maintenance-check-vsllm/` | 直连未建立连接；留存只读传输诊断 |
| `output/memory-maintenance-check-vsllm-proxy/` | 经现有环境代理可达，所配模型不可用 |
| `output/memory-maintenance-check-final/` | 同一已配置服务的可用模型被额度限制拒绝 |

未改用户保存的 API 配置；测试覆盖参数仅限运行器。未盲目重发未知计费请求、未重买成功请求。原始请求／失败文件不覆盖，历史预检对应当时版本；如需继续，先对当前代码生成独立目录的新预检。

## 5. 未完成及未做

真实模型纠错质量、跨批事件语义判断和实际预算标定仍缺可用且有额度的共享 API。代码执行上限是安全边界，不是“全量一定审完”的承诺；结果会列未审范围。

未用用户真实聊天做写入式生产 E2E；未做实体手机／其他 WebView 实测。没有启动新全量召回实验，没有改变「完整 USER＋AI 前三锚点邻块」检索方案，没有修改地图在途工作，没有提交或推送。

## 6. Review 修复与回归

2026-09-26：修复本轮审查确认的四处缺陷，未调用付费 API。

| 缺陷 | 修复边界 | 回归证据 |
| --- | --- | --- |
| 同轮工具失败仍接受 FinishReview | `runner.js` 拒绝错误所在响应中的收尾，集中错误解释引导模型读取失败结果；成功操作仍留在草稿，不重复执行 | 普通消息循环与会话式工具循环均先收到失败反馈，再修正 L0 并保存两侧；预算耗尽时不能保存 |
| 加载投影改变字段顺序，造成撤销假冲突 | 维护与原总结撤销共用结构比较：对象键顺序不影响相等，数组顺序与真实内容仍严格检查 | 当前格式 JSON 经序列化／重载投影后，同批多次维护及整批总结可撤销；实际改动人物数组仍拒绝覆盖 |
| LAN HTTP 缺少 randomUUID，结果无法保存 | 提交使用非安全上下文也可用的 getRandomValues 生成不透明收据 ID，并在失效索引前生成 | 移除 randomUUID、保留 getRandomValues 的能力模拟中连续提交成功，历史 ID 不重复；不是实体手机实测 |
| 旧维护在新总结保存通知前退出，丢失未审范围 | 调度器在最后一次冲突结果中暂存范围，仅下一批同聊天、已确认的新边界继承 | 手动／自动旧任务提前因基线变化退出，下一批均接回旧范围；取消、停用、换聊、重启、未知保存及传输失败不自动续发 |

以上回归位于[维护契约测试](../tests/memory-maintenance.test.js)。工程检查结果见第 2 节；真实语义验收仍是第 4 节的未测得状态，不能由这些回归测试替代。

离线 Gold 首轮失败记录及恢复：

- `pinned archive reuses cleaned requests…`：测试期间更新本验证文档，触发 `Prepared production source changed or unavailable`。停止修改后定点复跑通过。
- `actual process restart reuses saved…`：首轮 `binding-changed`；与其他 replay 检查并行复跑又出现 `manifest bundleHash 与归档 bundle 不一致`。检查确认 runner 共用 `scripts/.story-summary-replay-cache/story-summary-replay.bundle.mjs`；待其他检查全部结束、冻结工作区后串行定点复跑通过。
- 定点命令均为 `node --test --test-name-pattern='<对应测试名前缀>' scripts/gold-eval/tests/prepared-lifecycle.test.mjs`；使用该文件的离线供应商替身，没有实际外部付费请求。未改动指纹保护或评测工具，也不将首轮记为全绿。

## 7. 导入基线、锚点退役与统一保存修复

2026-09-26，针对后续 review 的两个 P1 和一个 P2。终态约束归目标设计第 6–7 节，代码落点归施工文档；本节记录真实验证，不另造兼容或审计分支。

### 修复及回归证据

[真实存储安全集成](../tests/memory-storage.test.js)使用实际总结数据层、HTTP 确认逻辑、隔离宿主文件和 IndexedDB，共 20 项通过：

- 真实导入构造 → 同基线两次维护 → 重载 → 回退：恢复维护前的 **4 个事件、5 条事实**，导入内容不清空，无维护时回退不可用。
- 真实上游边界型历史只在加载时变为基线；损坏生成逆操作拒绝当作基线。
- Agent 修改／删除锚点后，清空和原文失效扫描全部历史；退役标记、修改前后随原收据保存。重载并重新提取同 ID 后撤销，不覆盖新锚点，不阻断大总结撤销。有效锚点的人工冲突仍拒绝覆盖。
- 总结、历史、锚点、提取状态一起保存；服务端新状态即使宿主抛错也成功，已确认旧状态恢复整份草稿，回查失败／第三方新状态不擅自恢复。并发编辑保留并隔离；保存中不发布维护草稿，另一写入被拒绝。
- 保存未确认后禁止清空、锚点修改、提取及继续消费；重载只读实际服务端状态，不重发保存。切聊天不向新聊天安装旧结果。
- 删除／swipe 越过基线保留导入内容并记录来源失效。普通原文编辑的缓存清理或保存失败也停止使用旧锚点；安全标记保存失败仍运行期阻断。清空恢复时同时移除受失效来源影响的锚点，不误记为历史损坏。
- L0 提取结果暂存至整批完成；允许保留期间新总结，拒绝覆盖被编辑的 USER／AI 证据。缓存失效失败不改业务内容。

已移除锚点底层独立保存计时器，以及生成／导入／维护／撤销各自恢复内存的分支；用户开关和原文显隐这类会连带保存整份 metadata 的宿主入口同样检查保存门禁。索引补齐在写入前复核当前锚点，失败不会重买语义维护。

### 浏览器与工程检查

复用真实产品 iframe 和隔离宿主桥，以 Playwright 检查桌面 1280×800／手机视口 390×844、浅色／深色：退役记录仍可展开修改前后，说明失效原因；未确认保存时禁用复审／修索引，不再显示“修改已保存”的索引提示。两种视口均无横向溢出；不是实体手机或用户聊天写入测试。

截图位于 [`output/playwright/memory-maintenance-repair/`](../../../output/playwright/memory-maintenance-repair/)：`mobile-dark-retired.png`、`mobile-dark-unconfirmed.png`、`desktop-light-unconfirmed.png`。按前端设计技能仅补状态与必要解释，未改布局；按 Playwright 技能实测状态而非只检查节点存在。

本轮总结全套、运行时、零网络检索对齐、取消／响应／回滚存储、向量、replay 构建及生产入口 `write:false` 链接检查均通过，计数见第 2 节。最终 `npm run lint` 无错误或警告，`git diff --check` 通过。

本轮完整离线 Gold **168/168 通过**，无跳过；未与其他 replay 检查并行，执行期间未修改总结代码或文档，完成后才补写本条结果。跨进程重启复用和固定归档校验均通过，没有放宽指纹保护，也没有付费调用；第 6 节的历史失败记录仍保留。

本轮没有调用模型或改写用户聊天，没有启动全量召回实验，没有改检索方案，没有提交／推送，也没有修改地图、OS、画图的在途工作。第 4 节真实模型语义成绩仍未测得，本轮工程回归不能代替它。

## 8. 成果 UI 产品化

2026-09-26，仅处理用户授权的 UI 层。按前端设计技能重排信息层级，以事件／人物和修改内容为主，不展示 JSON、内部 ID、存储字段或索引操作文案。共享 API 定稿指引仍完整保留在展开入口。普通修改只展示变化字段；合并对照被接回的原片段与完整事件，相关前因引用以事件名称呈现。手机优先显示修改后，桌面并排对照。

新增的原文入口属于总结宿主的只读查询，不属于模型工具；使用现有维护记录限定聊天和截止楼层，返回当前原文及分页游标，不新存原文或另一套成果。界面明确提示原文可能后来编辑过。设置按钮明确为「保存设置」，成果无需再次保存。保存反馈不覆盖运行状态；状态更新保留已加载的历史页及未变化的展开内容。

### 实际检查

- `node --test modules/story-summary/tests/*.test.js`：**420/420 通过**。新增 5 项最低层契约测试覆盖：成果不改变原记录、只显示业务差异、各类记忆与弧光嵌套内容可读、原文完整分页且只读、跨聊天／撤销记录／越界读取拒绝。
- `npm run lint` 通过；最终变更文件 ESLint 与 `git diff --check` 通过。
- `npm run test:story-summary:runtime`、`npm run test:story-summary:bundle` 通过。生产宿主及 iframe 入口 esbuild `write:false` 链接检查通过，未改发布产物。
- Playwright CLI 驱动真实产品 iframe，隔离宿主桥提供代表性成果。桌面 **1280×900**、手机视口 **390×844**，深浅主题检查有内容、展开、空态、无修改、运行、失败、保存未确认及原文阅读。手机正文宽度与滚动宽度均为 **358px**；根字号从 16px 放大至 **24px** 后仍无横向溢出。
- 原文先读 10,000 字符，再续读至 20,000 字符；前后楼层可达，Escape 关闭后原成果保持展开。12 条历史加载完成后，状态刷新仍保留 12 条，不重新出现「更早记录」。开关保存未发送复审请求；手动复审与停止可用。
- 退役锚点的旧修改仍可回看；检索更新可单独重试。切换聊天的宿主通知会立即关闭原文并清理旧成果，未等下一次成果查询才清理。

截图位于 [`output/playwright/memory-maintenance-ui/`](../../../output/playwright/memory-maintenance-ui/)，包括 `desktop-light.png`、`desktop-light-detail.png`、`desktop-dark.png`、`mobile-dark-detail.png`、`mobile-source.png`、`mobile-light-empty.png`、`mobile-light-unchanged.png`、`mobile-dark-running.png`、`mobile-dark-failed.png`、`mobile-dark-unconfirmed.png`、`mobile-large-text.png`。

本轮没有修改 Agent 提示词、工具协议、调度、保存／撤销逻辑、记忆 schema 或检索方案，没有调用模型、修改用户聊天、提交或推送。真实语义质量依然是第 4 节的未测得状态；浏览器视口检查不是实体手机验收。

## 9. 工作能力与调用成本调整

2026-09-26，按目标设计第 5.4 节调整，不再把工具能执行当作 Agent 能独立工作的证明。

### 工程实现与回归

- 生成／L0／维护引用同一内容标准；过滤正文使用同一纯函数。生成提交携带规则指纹和过滤规则，自动维护使用该批冻结规则。标准不明的既有批次不伪造来源。
- 开场直接给待审记录、对应原文、旧事件及维护前后，只含提示词「What you have」逐项说明的字段；额外阅读与开场采用相同业务投影。事实 `since` 仅作为更新边界，弧光使用生成百分制；隐藏消息可读且不会被误判成另一种说话者；已撤回的事实与其他读取方一致，视为不存在。
- 自动工作只挑本批变化及需要重查的记录，背景中的旧事实可用于识别新矛盾，不全部算必审。超长材料有续读参数；程序分包、每包清理模型往返上下文。
- 工具收敛为 `ReadMemory`、`SearchSource`、范围式 `ReadSource`、`ReadReviewHistory`、`SubmitReview`。提交逐条生效：被拒条目连同原因回给模型，其余条目已生效，本包保持打开直至无被拒条目；同一记录以最后一次决定为准，未回答的目标记为未检查。模型只会撞上记录不存在（附候选编号）、楼号越界、因果引用非法、枚举非法及记录字段非法；不可编辑字段、无法识别的引用、缺失的 `(#X-Y)` 标记由程序处理并在 `notes` 中说明。引用可写段落编号、楼号或楼层范围；未给引用时引用该记录自身原文范围。
- 合并由程序保留最老身份，默认保留参与者和外部因果；确需修正因果集合时可在同一个合并操作中给完整值。既有统一保存、导入基线撤销、锚点退役和索引失效链路未绕过。

[工作契约测试](../tests/maintenance-work.test.js)用可控 adapter 验证：24 楼／16 条记忆完整注入后，一次模型请求位置即可提交两侧修改及跨批合并；输入材料无强制读取前置调用。它证明协议及调用安排，不证明模型判断正确。

实际检查：

| 检查 | 结果 |
| --- | --- |
| 总结全套 `node --test modules/story-summary/tests/*.test.js` | **430/430 通过**，无跳过；含 10 项新增工作／成本契约及既有保存、撤销、导入、UI 投影回归 |
| `npm run lint` | 通过；后续小修再跑受影响 JS 的 ESLint，通过 |
| 运行时、零网络对齐、取消／保存回归、replay 构建 | 全部通过；维护存储集成验证同批两次维护、真实导入撤销、旧向量失效、保存失败恢复和原文不变 |
| 向量检查 | 41/41 通过，供应商调用使用测试替身 |
| 宿主及 iframe 入口生产链接检查 | esbuild `write:false`，两个入口无警告通过，未改发布产物 |
| `git diff --check` | 通过 |

生产入口链接检查最初使用了不完整的宿主外置规则，误把本体 PDF／EPUB 库也编入，报 `JSZip/fs/http/https/url` 解析错误；改为按解析后绝对路径外置插件根目录之外的宿主模块后通过。未改本体或供应商代码以掩盖检查问题。

### 固定样本与真实调用

固定对象仍是第 4 节的 24 楼受控样本，不修改用户聊天，不扩展全量召回实验。预检增加开场材料、实现文件指纹和配置摘要，校验完整材料能放入开场；预检文件与请求日志均独占创建，不能覆盖旧运行。

- `output/memory-maintenance-batched/`：完成当时版本零 API 预检后，向已配置 VSLLM 服务发出 **1 次**受控样本请求，使用其模型列表中的 `gpt-5.4-mini`；服务拒绝，错误标识 `insufficient_user_quota`。没有模型正文、usage 或 `result.json`；请求及失败收据原样留存，没有重复提交。
- 主预设和另一个既有预设的只读模型列表检查均被凭据拒绝。没有修改用户共享配置、寻找外部凭据或购买新服务。
- `output/memory-maintenance-batched-final/`：最终代码独立零 API 预检，24 楼／16 条记忆一次完整提供，无材料续读缺口；开场系统提示、工具和消息合计 **33,504 字符**（序列化计数，不是 token）。该目录未再发起模型请求，不能与上一个目录混成一次成功运行。

真实修正、误改、漏改、事件语义合并和实际 token／费用仍**未测得**。P6 不标完成；全绿工程回归、一次请求的协议证明、开场字符数都不代替这些成绩。执行上限仍待真实样本标定。

本轮不改现行检索方案、地图、共享 Agent 配置或发布产物；保留已有 UI 工作。没有重做浏览器视觉验收（本轮未调整布局）、提交或推送。

## 10. 轻量化调整

2026-09-26，按本轮批准方案实施。领域所有权、持久化与删除路径见目标设计第 6–7 节；本节只记实际结果。

### 已落地

- 模型使用 `EditMemory({ edits, note? })`，支持修改、删除和完整事件合并。没有逐条 checks、强制引用或强制理由；空列表结束当前部分。整组字段与领域校验通过后才接受，错误给出条目及字段，任何一项失败均不部分生效。
- 本批原文驱动工作，即使记忆没变化或为空也会进入审查。开场提供范围、相关记忆、原文、事件目录及历史修改，超长材料有分页入口；所有材料、提示词、工具和往返均计入输入上限。
- 原文楼号从 1 开始；隐藏状态不影响读取，截止边界及过滤后的 story／raw 区分保留。只按已记录且匹配的指纹声明生成标准；导入、旧批次和无法确认的锚点不伪造标准。
- 每部分独立上下文、最多 8 次模型调用，通过后立即走既有统一确认保存入口，再刷新基线。停止、失败或新总结只丢弃未提交部分；超限部分记未完成，后续部分仍能保存。配置、保存未确认等整体故障停止本轮。
- 维护记录附于原总结历史，以 runId 分组；完成指纹用于手动续查。自动任务只续接未完成的自动范围，新的剧情始终进入，不因共享事实改变而自动重跑全部历史。手动任务被新总结打断后不从头重排。
- 审阅范围只记录真正随请求提供的原文和补读片段；准备后因输入上限未发送的内容、最后一次工具读取后尚未送回模型的内容不计入。读过或未修改不代表确认正确。
- 保留引用完整性、批次回滚、锚点退役、聊天与基线冲突检查、先失效向量后提交和只补索引的边界。没有增加创建记录或单项撤销。
- UI 沿用现有布局，只调整部分进度、停止说明、按运行分组和真实修改／未完成范围；不展示逐条“已检查”成绩。

### 工程与浏览器验证

| 检查 | 本轮结果 |
| --- | --- |
| 总结全套 `node --test --test-reporter=dot modules/story-summary/tests/*.test.js` | **431/431 通过**；含轻量协议、材料分页、独立预算、取消续查、新剧情、失效指纹及分组契约 |
| `npm run lint` | 通过；最终受影响 JS/MJS 再跑 ESLint，通过 |
| `npm run test:story-summary:runtime` | 通过；提示词对齐、运行释放、脏状态及重启通过 |
| `npm run test:story-summary:alignment` | 通过；零网络，现行检索与请求对齐，不测语义质量 |
| `npm run test:story-summary:cancel` | 通过；请求 12、响应 102、延迟 12、回滚 7、swipe 6；真实维护存储集成通过 |
| 维护存储集成 | 同批两次维护、真实导入基线撤销、旧向量失效、确认保存失败恢复、原文只读全部通过 |
| `npm run test:story-summary:bundle` | 通过 |
| 生产宿主及 iframe 入口链接 | esbuild `write:false`，2 个入口、0 警告，不改发布产物 |
| 生成行为一次性核对 | 当前和 HEAD 的总结／锚点 **11 个提示词常量完整文本相等**，没有留下文案快照测试 |
| `git diff --check` | 通过 |

新增回归保护具体可观察行为：一次响应完成两侧修改及事件合并；空修改后复审零调用；无效字段／后续领域错误使整组回滚；已保存部分在取消或后续超限后保留；续查跳过有效范围；新的未变旧事实仍入场；隐藏／raw 阅读、只查业务字段、长记录续读及标准未知；手动任务在自动开关关闭时也被新总结正确停止。既有导入、回滚、人工冲突、跨聊天和保存未确认测试保留。

对受影响 MJS 额外运行 ESLint 时发现验收脚本依赖隐式 Node `process`；改为显式内置模块导入后复跑通过。不修改配置来跳过检查。最终零 API 预检在该修正后重新生成，未覆盖任何实际调用收据。

Playwright CLI 驱动真实产品 iframe／隔离宿主桥，实测手机 **390×844／390×900**、桌面 **1280×900**，深浅主题。检查部分进度、停止、分组、空态、保存未确认禁用及原文打开。手机内容宽／滚动宽均为 **358px**，桌面均为 **630px**，无横向溢出。不是实体手机或用户真实聊天写入式 E2E。

截图位于仓库根目录 `output/playwright/`：`memory-light-mobile-running.png`、`memory-light-mobile-source.png`、`memory-light-390-light.png`、`memory-light-390-dark.png`、`memory-light-1280-light.png`、`memory-light-1280-dark.png`。

### 24 楼样本与真实模型缺口

样本仍为第 4 节的固定 24 楼、16 条记忆，没有扩大实验范围或修改用户聊天。

- `output/memory-maintenance-lightweight/`：完成当时版本的零 API 预检后，使用现有共享主预设 `newapi`、`claude-opus-4-6` 发出 **1 次请求**，凭据被拒绝（401）。无模型正文、无成功语义结果，没有重复 POST。请求、失败回执、journal 和 summary 原样保留。
- 已配置的 `smol-openai`、`glm` 仅做只读模型列表检查，均为无效凭据；`VSLLM` 的只读连接检查超时。没有改共享配置、寻找新凭据或购买新服务。
- `output/memory-maintenance-lightweight-final/preflight.json`：最终实现的独立零 API 预检。24 楼／16 条记忆完整开场，无必读续页缺口，提示词、工具、材料及剩余轮数消息合计 **31,297 个序列化字符**；不是 token 数。digest 为 `eb218a08a7631655d89be25734a6caf3a5887e80e78ba9f14851e5d33936583b`。该最终预检没有发起模型请求，不与前一个目录混成成功运行。

**真实模型的纠错、误改、漏改、语义合并、工具错误率及耗时均未测得。** 一次响应的确定性 adapter 测试只证明协议允许这样完成，不证明模型一次做对；约十秒是优化目标而非承诺。P6 保持未验收，不填写虚假的零误改或全量完成成绩。

本轮未改总结生成／锚点提取提示词文本或现行检索行为，未新增记录创建、单项撤销或 UI 重设计；未改地图等无关工作、用户原始聊天及共享 API 配置。未提交或推送。

## 11. 轻量化后续 review 修复

2026-09-26，处理后续 review 的事实冲突、任务复活、大记忆续查及提示词协议混入问题。产品规则与持久态归属见目标设计第 5–7 节；本节记录验证，不把工程测试等同于真实纠错质量。

### 修复与回归证据

- **事实不会因重复业务键在下次总结时丢失修正。** 生成、别名归一与维护共用事实键。维护在整组修改后的草稿校验，冲突给出条目、字段及两条记录；同组纠正归属、补全保留值并删除重复项，前后顺序均能成功。实际存储集成覆盖：冲突拒绝且磁盘不变 → 整理成功 → 后续空增量总结保留修正 → 回滚生成批次及维护后精确恢复原事实。
- **历史未完成项按真实完成记录扣除。** 多部分自动任务全部完成后，即使共享事实后来变化，新自动任务也只处理新楼层；原先失败部分后来手动完成，同样不被旧收据重新拉回。结果投影只有在对应范围确实完成时才消除未到达状态。
- **大记忆能保存进度。** 工作同时按原文片段与记录分工，不让每部分都承担整库。1000 条、每条 400 字符的事实和两楼原文：首部分保存后取消，复审从剩余记录继续；全部 1000 条各进入一次、每部分一次空修改响应完成，最终再复审零调用。单条记录改变仅重查输入失效的部分，新记录仍进入复审。单条内容本身过大仍受既定上限约束，未完成会保留，不保证任意大小均可一次处理。
- **保存中的交接不重排已完成范围。** 调度器保留尚未落盘的范围；确认保存后，剩余分工由历史接续。测试包含运行中、保存中、保存完成后收到新总结，以及取消到来后旧保存才确认的情况。
- **维护只引用生成内容规则。** 移除维护上下文里的增量输出协议、`newMoment`、`retracted` 及别名数组格式；锚点原文中的 `scene` 明确对应维护的 `semantic`。本轮按 writing-model-prompts 分离引用层与工具协议，没有修改生成全文或用当前文风重写未知版本记录的规则。

对应可复核测试：[工作与续查契约](../tests/maintenance-work.test.js)、[真实存储集成](../tests/memory-storage.test.js)、[调度契约](../tests/memory-maintenance.test.js)、[成果投影](../tests/maintenance-presentation.test.js)。

### 检查结果

| 检查 | 结果 |
| --- | --- |
| 总结全套 | **437/437 通过**，无跳过 |
| 全仓 lint、受影响 JS/MJS ESLint | 通过；首轮两处新注释格式警告已修正，再检查无警告 |
| 运行时、零网络检索对齐、取消与真实存储回归 | 通过 |
| replay 构建、生产宿主与 iframe 入口 | 通过；生产链接使用 esbuild `write:false`，0 警告 |
| 生成全文一次性核对 | 与 HEAD `a868b644` 的总结／锚点 **11 个提示词常量逐字相等**；不新增长期文案快照测试 |
| 差异空白检查 | `git diff --check` 通过 |

本轮没有改 UI 布局，未重复浏览器视觉验收；仅对受影响的成果分组／未完成投影做契约验证。此前浏览器证据仍见第 10 节，不冒充本轮新截图。

### 最终样本与未完成项

[`output/memory-maintenance-review-fixes/preflight.json`](../../../output/memory-maintenance-review-fixes/preflight.json) 为最终实现的零 API 预检：24 楼、16 条记忆完整开场，提示词／工具／材料／剩余轮数合计 **30,241 个序列化字符**，无必读续页缺口；digest 为 `19a1c13c7e19513571fd5961ddd297d2dae6191b3d0fedf9a33da2704294929a`。已有第 10 节收据及预检保留为历史记录，不用于冒充当前实现已验收。

预检首次误加了仅适用酒馆代理的 `--direct`，被脚本在网络调用及写入前拒绝；按现有共享配置重跑零 API 预检通过，未改供应商适配或用户配置。临时零 API 预检随最后的提示词选段修正重新生成，没有覆盖实际调用收据。

当前共享配置指纹与第 10 节已被凭据拒绝的请求一致，本轮没有重发该付费请求。**真实模型的纠错、误改、漏改、事件语义合并、工具错误率与耗时仍未测得，P6 不标完成。** 本轮未启动全量召回实验、未修改用户聊天、未新增创建或单项撤销、未改无关模块，未提交或推送。

## 12. 自主 Agent 改造

2026-09-26。第 10–11 节的分部分执行与完成矩阵已被本轮替换；那些测试结果只作历史记录，不代表当前架构。

### 实际改动

- 一次触发只有一个连续工具对话，toolChoice auto；正常文字回复结束。EditMemory 是普通修改工具，确认保存后可以继续读取和修改；空列表不终止。
- 同一回复读写混发只执行读取，修改返回未执行；不保存模型尚未见到读取结果就写出的旧参数。错误可在同一对话纠正。
- 删除 work-plan、progress 和分包调度字段。开场提供当前记忆、本批正文、生成变化和真实维护历史，超长内容通过目录及游标继续读取，不创建新任务。
- 每次实际修改独立确认保存；原子列表、业务键、事件完整合并、引用、锚点退役、先失效向量及批次回滚能力保留。
- runId 将编辑及最终回复／终止状态归组。原批次仍存在时收尾只追加历史，不覆盖新总结；聊天已切换或原批次消失时不越权补写，无收尾的已保存组显示未收尾。
- v1 分包收据在加载入口转换为 v2，仅移除派工元数据；以旧代码实际产生的冻结样本验证逆操作不丢失。没有独立运行库，也没有重启恢复付费任务。
- 依据 writing-model-prompts 重组职责、时间概念、材料、判断和工具说明；按前端技能保留原布局，仅替换真实运行状态和成果语义。

### 工程验证

| 检查 | 本轮结果 |
| --- | --- |
| 总结全套测试 | **432/432 通过**，无跳过 |
| 连续工具对话 | 同一 run 完成读取→结果→编辑确认→继续读取／编辑→正常回复；普通及原生 session adapter 两种协议均通过确定性测试 |
| 1000 楼 × 1500 字、150 事实、200 事件 | 一个开场、一个 run；开场为最新北京状态及最近 20 楼，旧长沙正文按需读取；没有宿主预生成部分或重复注入事实 |
| 读写、上限与中断 | 混发拒写、无效字段整组拒绝、多编辑列表拒绝、空修改非终止、取消及 48 次／24 万字符边界通过；已确认修改保留 |
| 安全回归 | 多次修改逆序回滚、事件引用、事实业务键、人工冲突、跨聊天、原文只读、隐藏／过滤视图、保存未确认、锚点退役、导入基线通过 |
| 新总结调度 | 自动触发范围合并；手动不重排；同截止楼层重新总结也中止旧 run |
| lint | 全仓 npm run lint 通过；最终受影响 JS／MJS 再跑 ESLint 无错误、无警告 |
| runtime／alignment | 通过；alignment 零网络，不冒充语义测评 |
| cancel／真实存储集成 | 请求 12、响应 102、延迟 12、回滚 7、swipe 6；同批两次维护、导入基线撤销、旧向量失效、确认保存失败恢复通过 |
| 构建 | replay bundle 通过；宿主／iframe 两入口 esbuild write:false，2 个输出、0 警告 |
| 生成全文 | 一次性 acorn／vm 核对，HEAD 的 11 个总结／锚点提示词常量逐字相等；未增加长期文案快照测试 |
| git diff --check | 通过 |

程序测试只证明最新状态正确入场及执行契约，**不证明真实模型不会把北京误改为长沙**。

### 浏览器实测

Playwright CLI 驱动实际产品 iframe 与隔离宿主桥；桌面 1280×900、手机 390×844／900，深浅主题。检查运行动作、停止、已保存成果、限额、失败、保存未确认禁用、无修改、空态、展开合并、原文打开／续读／返回。
手机内容宽／滚动宽均为 358px，桌面均为 630px，无横向溢出。不是实体手机或用户真实聊天的写入式 E2E。

截图在仓库根目录 output/playwright：

- memory-autonomous-mobile-running.png、memory-autonomous-mobile-source.png
- memory-autonomous-390-light.png、memory-autonomous-390-dark.png
- memory-autonomous-1280-light.png、memory-autonomous-1280-dark.png

### 24 楼零 API 预检与真实结果缺口

最终预检：output/memory-maintenance-autonomous-final/preflight.json。
24 楼／16 条记忆完整开场，提示词、工具、材料及剩余调用数合计 **30,300 个序列化字符**；
digest：b673f448091f569dd1d090c2f85bcddaa7c4d094cfad6c2f9fe7e8bb160bfb83。
这是字符数，不是 token 或账单；本轮没有模型 POST。

共享主预设指纹仍与第 10 节被拒绝的请求相同；本轮只读模型列表检查仍返回 401。记录见 output/memory-maintenance-autonomous-final/availability.json。
既有请求、失败回执和原始样本均保留，不重发已知失败或未知计费请求。
**真实模型的两侧纠错、误改、漏改、语义合并、工具错误率和耗时仍未取得，P6 不标完成。**

本轮不新增创建、单项撤销、UI 重设计、新 API 配置或全量召回实验；不修改用户聊天、生成／提取全文、检索及无关模块，不提交推送。

## 13. 自动维护入口收敛

2026-09-26。本节取代第 12 节中手动复审的入口与上下文约定，其余自主 run 和保存边界不变。

### 实际改动

- 删除「复审一次」按钮、宿主消息处理、host.review、scheduler.review、手动上下文分支及相关状态字段；不是隐藏按钮后保留后台启动路径。
- 唯一启动来源为总结确认保存后的通知，用户发起总结和自动总结继续共用该通知。授权默认关闭；开启并保存不会立即调用模型。
- 开场 task 仅保留触发范围 start／cutoff；合并被中断触发范围后仍是一段连续对话。当前状态、按需查阅、修改保存、停止保留成果不变。
- 新收据不再写 mode；已有保留收据在加载入口清理该字段，修改差量、审阅范围、锚点退役和撤销链保留。
- 按 writing-model-prompts 对齐实际材料字段，按 frontend-design 清理手动动作及空态文案，未重设计页面。未来用户主动入口应是对话，本轮没有实现或预埋。

### 验证结果

- 总结测试 **431/431 通过**，无跳过；删除失去产品意义的手动模式测试，保留并更新自动关闭、触发合并、同截止重新总结、冲突及重启契约。
- 全仓 lint 通过；最终受影响文件 ESLint 无警告，git diff --check 通过。
- runtime、alignment、cancel／存储集成、replay bundle 全部通过；宿主与 iframe 两入口 esbuild write:false，2 个输出、0 警告。
- 存储回归继续覆盖同批两次维护、导入基线撤销、旧向量失效、原文不改和确认保存失败恢复。冻结 v1 收据转换后仍可精确撤销。
- Playwright 使用实际 iframe 与隔离宿主，实测 1280×900、390×900，默认深浅主题：空态无手动执行按钮；授权保存只有配置请求；停止发送取消消息，已有成果保留并可展开；保存未确认无可执行入口。页面宽／滚动宽分别为 630／630、358／358，无横向溢出。
- 截图：output/playwright/memory-auto-only-1280-default.png、memory-auto-only-1280-dark.png、memory-auto-only-390-default.png、memory-auto-only-390-dark.png、memory-auto-only-stopped.png。截图关闭过渡动画以验收最终画面，非实体手机测试。
- 24 楼零 API 预检：output/memory-maintenance-auto-only/preflight.json；完整输入 30,176 个序列化字符，digest 为 1448c4b0ad350a5bbd3ac8dfe2c64e271b39f609147ae0e95104c4c2407774ba。

本轮不请求真实模型，不改变生成、锚点提取或检索行为，不修改用户聊天，不提交推送。此前真实模型质量与耗时的验收缺口仍在，不能以本轮入口测试或预检代替。

## 14. 范围闭环与长上下文续行

2026-09-26。本节记录范围闭环与长上下文续行验收；后续按批次选材的调整见第 15 节。前述章节保留为历次记录，不作为现行材料、范围或限额契约。

### 实际交付

- 开场只含范围任务和当前记忆；正文按需查，删除生成差量模式、重复事件目录及模型侧维护历史工具。业务投影附稳定标识、维护归属、可确认的生成标准；分页使用稳定游标。
- CompleteMaintenance 确认后在原总结历史追加空修改收据；有效范围由 completion 减 invalidated 后合并。重复声明不新增历史；普通回复不冒充完成。未实际送达或未读完的现存记忆阻止范围确认，并返回续读位置。
- 旧事实被新总结更新时归新批次；未知旧记录归固定起始基线。人工修改、来源失效与迟到锚点通过统一提交边界退役进度；自身修改不误退役。原批次撤销及替换导入不保留被撤内容的完成。
- 同一个 run 内按 token 计完整输入；主请求和独立摘要请求共用安全计数。压缩保留完整工具交互，成功并重新计数后才替换；新协议会话不改变 runId、聊天或完成范围。失败、取消、保存未确认保留已确认成果，不另开 run。
- 页面沿用原布局，仅显示范围、动作、真实修改和终止状态；有“整理工作上下文”，无执行按钮、逐项正确率或新 API 表单。提示词按 writing-model-prompts 分清材料、时间、判断与工具；界面按 frontend-design 保留原有阅读层级。

### 工程验证

| 检查 | 实际结果 |
| --- | --- |
| 总结测试 | **449/449 通过**，无跳过；最终 runner 调整后其范围／续行相关 28 项再次通过 |
| 共享计数与供应商协议 | **34/34 通过**：原生 Google 会话、Anthropic 工具组、思考／签名重放计数与取消 |
| 范围闭环 | 无修改确认、重复确认、未读拒绝、分段文本完整送达、读取混发拒写、失败修改阻止完成、重启接续、外部冲突均通过 |
| 归属与存储 | 新批次更新旧事实、固定导入基线、人工编辑、迟到锚点、清理后同 ID 重建、导入替换、回滚、保存旧状态／未确认通过 |
| 长上下文 | 53 次连续交互仍同一 run；普通／原生 session 两种循环多次压缩，完整工具对和已完成范围保持；摘要分段输入也受 token 限制；失败、取消和总次数限制保留成果 |
| 长聊天模拟 | 1000／5000 楼正文、150 事实、200 事件：开场只有 task、memory、memoryDirectory；无默认历史正文、重复事件目录、维护流水或宿主预生成任务 |
| lint | 全仓 npm run lint 通过；最终受影响 JS／MJS 再检，无错误／警告 |
| runtime／alignment | 通过；alignment 零网络且明确 qualityMeasured=false |
| cancel／真实存储边界 | 请求 12、响应 102、延迟 12、回滚 7、swipe 6；同批维护、导入基线撤销、向量失效、原文不改及确认失败恢复通过 |
| 构建 | replay bundle 通过；宿主／iframe 两入口 esbuild write:false：2 输出、0 警告 |
| 生成全文 | 一次性 acorn／vm 核对：与 HEAD 的 11 个总结／锚点提示词常量逐字一致；未留长期文案快照测试 |
| diff 检查 | git diff --check 通过 |

输入计数使用共享能力：有有效 tokenizer token IDs 时标 tokenizer，服务不可用时标 estimated。前者测的是主机文本／工具及重放内容的投影，不是供应商实际账单；两者均与字符数区分。所用接口仍需支持输入预算加共享输出容量，未按模型名猜容量。

### 页面实测

Playwright CLI 驱动实际总结 iframe 与隔离宿主：1280×900、390×900，默认深浅主题；检查范围、部分完成、压缩中、停止、展开修改、原文打开／返回、保存未确认、空态和授权保存。手机范围区域 clientWidth／scrollWidth 为 358／358，无横向溢出；没有手动执行入口。最后的授权保存仅发 SAVE_PANEL_CONFIG，没有维护执行消息。

截图位于 output/playwright：memory-ranges-1280-default.png、memory-ranges-1280-dark.png、memory-ranges-390-default.png、memory-ranges-390-dark.png，以及 memory-ranges-compacting.png、memory-ranges-source.png、memory-ranges-unconfirmed.png、memory-ranges-empty.png。不是实体手机或真实用户聊天写入式 E2E。

### 24 楼样本与仍缺的质量验收

- 最终零 API 预检：output/memory-maintenance-ranges-final/preflight.json。完整输入估算 **9,108 tokens**；24 楼／16 条现存记忆全部入场；正文由模型按需读。digest：5f7820ee1244fdf498bd37a6473bd5677790e11b2bc3e8e09d983a2d10de95d1。
- 本轮只读共享服务的 models 可用性检查仍被拒绝（401）；记录：output/memory-maintenance-ranges/availability.json。主预设为 claude-opus-4-6，模型请求为 0。未重发先前已知失败或未知计费请求，也未修改凭据。
- 因此真实模型的大总结／锚点纠错、误改、漏改、语义合并质量、工具错误及耗时**仍未测得**。程序测试不证明模型不会将北京误改回长沙；该质量验收不标完成。
- 原样本、先前收据和已有工作区修改保留。本轮不新增创建、单项撤销、手动入口、API 设置或全量召回实验；未改变生成、锚点提取和检索行为，不提交推送。

## 15. 按待维护批次选材

2026-09-26。本节记录本轮实际结果；现行选材、范围读取及预算契约统一见目标设计第 5.6 节。

### 交付与边界

- 开场与 ReadMemory 共用责任范围过滤；前批衔接范围从有效历史边界派生，并扣除待维护部分。只提供去重的当前值，没有新增历史快照、持久化字段或调度任务。
- 新增 ranges 参数，续读保留范围、分类、业务查询及稳定游标；目录仅统计所选内容。长记录可完整拼回，删除和合并前页条目后不跳过后页。
- 开场包含目录和范围的完整材料先估算、再使用共享计数复核；超限时重装完整记录。被移出开场的记录不会误记成已送达。完成校验仍覆盖申请范围的全部现存责任记录，不强制重审前批背景。
- 按 writing-model-prompts 对齐现有材料、责任时间与工具参数；没有增加填表、查询进度或结束确认工具。
- 相对本轮开工快照，仅修改维护选材／工具／提示词／预算、相关测试及三份文档。生成、锚点提取、检索、UI 和用户聊天未改；原有工作区修改及收据保留；不提交推送。

### 工程验证

| 检查 | 实际结果 |
| --- | --- |
| 总结测试 | **457/457 通过**，无跳过；本轮新增 8 项选材／读取契约测试 |
| 两批 40 楼 | 查询 10–30 返回两批的当前事件和事实，L0 只含 10–30；重合范围不重复记录，筛选条件取交集 |
| 不连续范围 | 非固定批长、迟到／重建锚点形成的失效子范围、前批扣除待维护范围、跳过无关已完成中段均通过 |
| 旧记录与基线 | 最新批次更新的旧事实只出现一次且为当前值；未知记录保持起始基线归属 |
| 长聊天 | 1000／5000 楼已完成历史各只注入最后两批的 **44 条**记录：2 事件、2 事实、40 锚点；选材条数不随已完成历史增长 |
| 大材料与续读 | 超长事实可按 scoped next 无损读完；未读完阻止完成。模拟 tokenizer 计数大于估算时，模型请求前缩装且完成校验不误认被移除记录已送达 |
| 进度与压缩 | 完成返回、重启后的开场和压缩后任务使用同一派生规则；普通／供应商会话多次压缩后保留前批衔接范围 |
| 原有安全回归 | 读写混发、修改失败阻止完成、取消保留成果、保存未确认、人工冲突、跨聊天、完整合并与撤销继续通过 |
| lint | 全仓 npm run lint 通过，无新增 lint 错误 |
| runtime／alignment | 通过；alignment 零网络且 qualityMeasured=false |
| cancel／存储 | 请求 12、响应 102、延迟 12、回滚 7、swipe 6；同批维护、导入基线撤销、旧向量失效、原文不改及保存失败恢复通过 |
| 构建 | replay bundle 通过；按插件／宿主边界打包宿主和 iframe，esbuild write:false：2 输出、0 警告 |
| 生成全文 | 一次性 acorn／vm 核对 11 个总结／锚点提示词，与 HEAD 完全一致；未新增长期字符串快照测试 |
| 差异检查 | git diff --check 通过；开工文件哈希对照确认生成／检索／UI 未改 |

额外尝试把 SillyTavern 本体一并打包时，已有 epub.min.js／jszip.min.js 产生 3 条 CommonJS-in-ESM 警告；随后按真实插件边界将宿主作为外部模块完成上述构建检查，没有修改或抑制第三方库代码。本轮未重设计 UI，也未重复浏览器截图验收。

### 固定样本与质量缺口

- 零 API 预检：output/memory-maintenance-selection/preflight.json；24 楼、16 条当前记忆完整入场，材料估算 **1,479 tokens**，加提示词和工具后的完整输入估算 **9,472 tokens**。
- digest：2d56894c8363c32d07106797cf9da0f6751dd494d215248f7999699726128626。预检冻结了共享配置指纹、当前实现、材料和工具；原文按需读取。
- 共享服务只读 GET models 检查仍返回 401，配置指纹与上轮相同；记录位于 output/memory-maintenance-selection/availability.json。**模型请求 0**，未重发已知失败或未知计费的请求，未改凭据。
- 因此真实模型的纠错、误改、漏改、语义合并、工具错误和耗时仍未验收。上述程序结果只证明材料选择与运行安全契约，不证明模型判断质量。

## 16. 工具易用性与同 run 更新

2026-09-26。本节替代第 14–15 节中的“读写混发拒写”“只能一个修改调用”“逐条送达门槛”和“新总结停止旧 run”契约；旧节的实际测试结果保留为历史，不代表当前行为。本节的同 run 新总结接入已由第 17 节替代。

### 调整与依据

- 对照 ebook 的编辑实现和 draw 的参数处理，将无歧义的格式整理放在维护工具入口，仍由领域层验证业务内容。普通修改可省略 kind；支持数字／布尔字符串、枚举空白及大小写、单项列表。错误返回条目、字段及可接受的字段／类型／边界，不猜正文或修改目标，不吞未知字段。
- 读取与多次修改顺序执行；每份修改列表分别原子校验、确认保存、返回结果。完成声明置后，任何修改失败都不会顺带登记完成。重复写相同值为无变化。
- 删除记忆逐条送达追踪和完成阅读门槛。CompleteMaintenance 由 Agent 声明，保留范围、基线、存储确认；原文审阅范围追踪仍在。
- 新总结通过 refresh.js 在工具边界更新同一个会话。保存中的总结先等待确认；确认等待可以取消而不取消他人的保存。旧回复的写入不执行，模型看到新材料后继续；对话、runId、已保存成果与请求计数不重置。
- 通过生成历史识别追加批次，接收同截止的总结更新通知；导入、回滚、原文变更不冒充新总结。最终收尾保存期间的新触发等当前结束后执行，安全上限不会自动重开当前已排入的工作。
- 按 writing-model-prompts 对齐当前行为、材料及返回字段；保留“依赖读取结果的修改须等看到结果再决定”，不再用工具硬门槛代替模型判断。
- 未改生成、锚点提取、检索、UI 或共享 API 配置；未新增持久化实体、未提交推送。

### 实际验证

| 检查 | 结果 |
| --- | --- |
| 总结测试 | **464/464 通过**，无跳过；最后一轮包含存储确认等待和取消 |
| 工具易用性 | 普通修改省略 kind、数字／布尔字符串、枚举格式、单项列表可执行；业务正文保留原样；无效字段整组拒绝并返回可用字段 |
| 多工具调用 | 同回复读取／修改顺序执行；多个修改独立保存，其中一项失败不吞其他结果；失败阻止完成；逆操作恢复精确一致 |
| 范围声明 | 不要求逐条读取；空修改不结束 run，普通回复不冒充范围完成；重复声明、保存失败、外部冲突仍正确 |
| 新总结接入 | 普通／供应商会话两种模式保留同一对话；响应期间及保存竞态更新材料、拒旧写入；同 cutoff 更新、正常回复恰逢新总结、导入和源文冲突均覆盖 |
| 调度 | 思考／保存／索引期间合并更新，不中止模型；关闭与切聊天取消边界保留；最终收尾期间的新触发不丢失；上限不自动重跑 |
| 原有回归 | 1000／5000 楼选材、长记录分页、超过 48 次交互、压缩、事件合并、引用、批次撤销、锚点退役和未确认保存通过 |
| lint | 全仓 npm run lint 通过；最后修改后重新运行受影响维护、存储和测试文件 eslint 通过 |
| runtime／alignment | 通过；alignment networkCalls=0、qualityMeasured=false |
| cancel／存储 | 请求 12、响应 102、延迟 12、回滚 7、swipe 6；维护存储检查通过，旧向量失效、原文不改、保存失败恢复保持 |
| 构建 | replay bundle 通过；宿主／iframe 按插件边界 esbuild write:false，2 输出、0 警告 |
| 生成全文 | 一次性 acorn／vm 比较：11 个总结／锚点提示词与 HEAD 逐字一致；无长期文案快照测试 |

### 固定样本与质量缺口

- 最终零 API 预检：output/memory-maintenance-tool-usability/preflight.json；24 楼，16 条当前记忆完整入场；材料估算 **1,479 tokens**，完整输入估算 **9,724 tokens**。
- 最终实现 digest：10c7370a8bf00e7a0802040d150f2f488689c38f48da4fd7e42402981057db1f。冻结内容包含新增刷新逻辑、宿主与统一保存边界。
- 只读 GET models 再次返回 401，配置指纹未变；收据见同目录 availability.json。**模型请求 0**，未重发付费请求。
- 真实纠错、误改、漏改、合并质量、模型工具错误率和耗时仍是验收缺口。程序测试不证明模型能够在十秒内完成或永不出错。

## 17. 新总结排队与定点冲突恢复

2026-09-26。产品契约见目标设计第 5.3、6.2–6.3 节。本轮移除新总结并入当前会话的路径，保留自主读写、工具容错及原子保存。本节“重读后重提可保存”的测试未模拟 mergeFacts 同步更新 since，不能证明真实新总结流程；此行为及测试由第 18 节修正。

### 实际验证

| 检查 | 结果 |
| --- | --- |
| 总结测试 | **469/469 通过**，无跳过 |
| 队列到执行器 | 当前 run 的结论确认保存后才启动不同 runId 的下一 run；新开场带新边界和当前值，已完成范围继续保留 |
| 调度 | 思考、保存、索引、最终收据阶段的新总结仅进 pending；多个等待批次合并，旧 cutoff 不变；关闭／取消清除待执行任务，重启不恢复请求 |
| 冲突恢复 | 普通与供应商会话两种模式：旧草稿返回 memory_updated 及具体记录；ReadMemory 定点重读后重提可保存；对话仅有一份开场，不因新批次重建会话 |
| 结论竞态 | 正常回复恰逢新总结，仅一次该结论响应便结束；结论与旧 cutoff 落入同 run 的历史分组 |
| 合并与数据安全 | 无关新事实和后续锚点保留；引用重定向涉及的记录也校验冲突；新增事实带来的业务键冲突可回喂修正；跨批穿插修改后精确撤销 |
| 读取一致性 | 只更新实际返回的记录基线；保存不默认确认其他新值。长记录续读期间发生改动，拒绝混拼新旧片段，重读可完整取得新值 |
| 既有回归 | 聊天隔离、原文只读、取消保留成果、完成范围、压缩、未确认保存、向量失效与索引修补通过 |
| lint | 全仓 npm run lint 通过；末轮维护及测试文件 eslint、git diff --check 通过 |
| runtime／alignment | 通过；alignment networkCalls=0、qualityMeasured=false |
| cancel／存储 | 请求 12、响应 102、延迟 12、回滚 7、swipe 6；维护存储检查通过 |
| 构建 | replay bundle 通过；宿主／iframe 以插件真实边界 esbuild write:false，2 输出、0 警告 |
| 生成全文 | 一次性 acorn／vm 核对：11 个总结／锚点提示词与 HEAD 逐字一致 |

### 固定样本与边界

- 24 楼零 API 预检：output/memory-maintenance-queued-runs/preflight.json，材料估算 1,479 tokens，完整输入估算 9,794 tokens；networkCalls=0。
- 冻结 digest：77d260f3d3f9aded4ca8a52db14399052f957d0f2b865b02ceab06fa15f4192d。预检文件清单已移除 refresh.js，并接入当前读取端口。
- 本轮未发起真实模型请求，不新增语义质量或耗时结论；之前的真实质量验收缺口仍保留。
- 未改生成、锚点提取、检索、UI 或共享 API 配置；没有新增持久化字段或依赖，没有提交推送，也未清理此前工作区产物。

## 18. 更新记录本轮跳过

2026-09-26。按目标设计第 5.3 节修正 memory_updated 的处理方向。

- 错误集中说明：列出的记录本轮跳过，继续维护其他记忆；整份修改列表未保存，无关修改可单独重提。工具说明移除“重读后重试”引导，完成失败也不再要求重新修改应跳过的记录。
- 复用既有批次归属，在草稿校验和提交时识别本次截止之后的责任范围；重读不把新批次记录变成旧 run 的可编辑目标。无新增持久化状态。
- 修正 nextSummary 测试数据：模拟 mergeFacts 更新既有事实时将 since 设为新总结楼层，不再只改事实正文。

实际验证：

- 总结测试 **470/470 通过**，无跳过。
- 普通／供应商会话均覆盖：旧 run 24 楼，新总结更新事实到 26 楼 → memory_updated 返回具体记录 → 原列表整组不保存 → 无关修改重提成功 → 原范围可完成 → 新范围保持待维护，下一 run 能处理该事实。
- 重读后再次修改或删除新批次事实，仍明确返回 memory_updated，不改报 source_boundary，不产生草稿或完成记录。
- 运行时、cancel／存储、replay bundle 通过；宿主与 iframe 的实际插件边界构建为 2 输出、0 警告。
- 全仓 lint 及末轮受影响文件 eslint、git diff --check 通过。

未改总结生成、锚点提取、检索、UI、调度策略或输入预算；未提交推送。本轮未发起真实模型请求，工程测试不证明模型判断质量或耗时。

## 19. 进度收据、事件规范化与检索分页回归

2026-09-26。修复本轮审查的三项问题，不改变 Agent 协议或新增持久化状态。

- `maintenance/commit.js` 仅在大总结内容受影响时更新时间；进度、收尾说明及仅锚点修改不使生成器的总结基线失效。
- `data/events.js` 提供事件参与者／前因列表的规范化，加载入口与维护草稿共用。修改先校验字段，再规范化、生成差量；收据与保存正文一致，非法输入仍拒绝，撤销冲突检查未放松。
- `maintenance/history.js` 统一派生全聊天维护影响集合，成果查询与索引重试共用。成果分页不再决定检索状态；已删除记录和已退役锚点继续按现有规则排除。

实际验证：

- 总结测试 **478/478 通过**，无跳过；存储集成 **32/32 通过**。
- 新增 7 项存储边界回归：生成等待期间分别保存完成范围、收尾、锚点和大总结修正；前三者正常提交新总结且保留维护收据，后者拒绝旧生成结果。事件普通编辑／合并中的参与者和前因去除首尾空白后，磁盘、重载和逆操作一致。11 次维护运行中最旧修正的事件／锚点缺向量，首页与旧页均显示待更新；修补后两页均恢复正常。
- 运行时检查与 replay bundle 通过；取消／存储检查通过：请求 12、响应 102、延迟 12、回滚 7、swipe 6；维护存储检查仍验证旧向量失效、原文只读及确认失败恢复。
- 宿主与 iframe 入口按插件边界 esbuild `write:false` 构建，2 输出、0 警告。全仓 `npm run lint`、受影响文件 eslint、`git diff --check` 通过。

模型与 embedding 使用离线替身，元数据确认及 IndexedDB 使用现有存储测试边界。未发起真实模型请求，不增加语义质量结论；未改提示词、总结生成／锚点提取逻辑、检索排序或 UI，不提交推送，也未清理既有工作区产物。
