# Atlas 通用空间地图：施工文档

状态：空间模型、工具、升级入口、投影和生产界面已实现；确定性检查与浏览器结果见第 6 节。真实模型质量验收及用户真实存档补测尚未执行，不宣称第一版全部验收完成。

规格唯一来源：[最终设计](map-atlas-geography-design.md)。本文只负责当前代码落点、施工依赖、检查方法与验收证据。范围变更先改规格，施工计划引用对应章节，不另写一份空间契约。

## 1. 交付与非交付范围

本轮施工完成的标准是最终设计第 10 节全部必要验收有证据，不是“森林能显示”或“工具可以调用”。

- 全部空间事实继续进入原 Map 分区；不建立试验地形分区、浏览器本地地图副本或长期双模型。
- 自然、城市、海洋、人工结构、太空与幻想空间按同一实现交付，不拆成未来题材包。
- Scene 内部、维护触发和存储协议保持既定边界，详见最终设计第 9 节。
- 本文落盘不授权操作用户真实地图、发布、推送或发起付费模型调用。实现时区分隔离 fixture 验证、真实存档样本与真实模型验收。

## 2. 当前基线与施工落点

下表保留施工前的缺口与职责分配，供追溯；实际完成状态以第 6 节为准，不代表当前运行代码仍有这些缺口。

| 现有位置 | 当前基础／缺口 | 本轮职责 |
| --- | --- | --- |
| [types](../domains/map/types.ts)、[invariants](../domains/map/invariants.ts)、[edit](../domains/map/edit.ts)、[state](../domains/map/state.ts) | Atlas 现有地点、连接、人物；position 未显式引用独立框架，terrain 只是地点标签 | 接入当前空间模型、引用与数值不变量、原子领域编辑和初始状态 |
| [partition](../apps/map/partition.ts)、[service](../apps/map/application/service.ts) | 分区直接解析当前 Map 格式，已有保存／revision 流程 | 读取／导入边界的一次转换，当前格式写入；不改 Kernel 协议 |
| [hierarchy](../domains/map/hierarchy.ts)、[map-browse](../apps/map/ui/map-browse.ts)、[world-map](../apps/map/ui/world-map.ts) | 已区分世界／地区浏览；布局仍有直接父级坐标判断和示意排列 | 浏览事实与几何投影归领域，UI 消费单一结果 |
| [atlas-location-compiler](../apps/map/tools/atlas-location-compiler.ts)、[atlas-intent-compiler](../apps/map/tools/atlas-intent-compiler.ts) | 已有地点层级关联组，不包含新空间依赖 | 扩展最终候选组验证、映射／换框架意图及删除依赖 |
| [atlas-reader](../apps/map/tools/atlas-reader.ts)、[共享工具契约](../apps/map/tools/tool-contract.ts) | 已有分页、工具回执、Atlas 编辑入口 | 新事实的有界投影、共同 Schema、真实读改回环 |
| [维护 session](../apps/map/maintenance/session.ts)、[维护指导](../apps/map/maintenance/prompt.ts) | 已有初始资料与正常维护工具循环 | 同一投影注入、空间维护决策和可执行示例 |
| [管理员契约](../apps/map/management/tool-contract.ts)、[管理员指导](../apps/map/management/prompt.ts)、[participant](../apps/map/management/participant.ts) | 管理员是另一个真实消费者，Atlas 描述仍有覆盖入口 | 引用共享内容契约，只保留自身分页和保存包装 |
| [MapApp](../apps/map/ui/MapApp.vue)、[MapAtlas](../apps/map/ui/MapAtlas.vue)、[MapViewport](../apps/map/ui/MapViewport.vue) | 已有三层入口和视口；地图挂载仍依赖地点数量 | 空间投影挂载、真实范围全图、未定位详情与新绘制入口 |
| [MapSearch](../apps/map/ui/MapSearch.vue)、[MapPlaceDetail](../apps/map/ui/MapPlaceDetail.vue)、[map-copy](../apps/map/ui/map-copy.ts) | 已有分范围搜索与详情、集中交互文案 | 全部地点与可定位子集分离，按语义原因展示，不新增整理弹窗 |
| [Scene 几何](../apps/map/ui/scene-geometry.ts)、[Scene 出口](../apps/map/ui/MapSceneView.vue)、[三维组装](../apps/map/ui/three/scene3d-model.ts) | 同一 Scene 支持二维／三维，程序负责造型材质；森林配额依赖集合 | 仅借鉴已验证分工和语义相同的基础能力，不复用不满足跨图稳定性的配额 |
| [主 RP 投影](../domains/map/projection.ts)、[prompt runtime](../apps/map/host/prompt-runtime.ts) | 已有有界只读空间摘要 | 当前模型类型适配与回归，不将地形几何、装饰或 Scene 注入主 RP |

### 已落地的职责位置

以下目录已创建，实际拆文件只围绕列明职责，没有引入通用地图框架。

- `domains/map/space/`：空间类型与语义、框架转换、空间校验、Atlas 浏览投影。各职责独立，均不依赖 Vue、SVG、three 或工具回执。
- `domains/map/upgrades/`：受支持输入格式的冻结定义及独立转换；只由解析边界调用，不被普通投影或 renderer 导入。
- `apps/map/tools/atlas-spatial-compiler.ts`：把高层空间意图转换成候选修改，接入统一关联组；不能自己再保存或复制领域校验。
- `apps/map/ui/atlas/`：空间图层编排、几何／掩码适配、材质和稳定装饰；不拥有存储、地点层级或到访推导。
- `tests/fixtures/`：当前工具输入样例与真实历史存档样本分开标识，不能拿手写的当前输入充当历史版本证据。

具体 Schema 字段和上限由领域类型与校验定义，两个工具消费者引用；本文不复制 JSON 接口。

## 3. 施工顺序与关卡

顺序为 S0 → S1 → S2 → S3 → S4 → S5 → S6 → S7。可以做阶段性本地验证，但不能发布半个新模型，也不能把未完成项目转成“以后再拆”。

### S0：冻结输入与复核基线

- [x] 检查 dirty worktree，登记本次可改范围，保留无关用户修改；不回退已有地图修复。
- [x] 确认所有 Map 解析、导入、序列化入口，以及正常维护、重建和管理员的调用链。
- [x] 按最终设计第 7 节冻结现行旧格式及字段契约，登记类型、校验和容量边界；不以某几份样本反推支持范围。
- [x] 为冻结格式建立结构与边界测试，覆盖直接父级坐标、嵌套局部坐标、无坐标地点、Scene、到访事实和满容量数据；测试构造输入与用户补充存档明确区分来源。
- [x] 运行第 4 节适用基线检查，记录原有失败与环境问题，不能计作本次新功能结果。

关卡：现行输入含义与调用边界有证据，能够依冻结格式定义完整转换；不猜测旧坐标关系。真实存档的补充确认遵循最终设计第 7 节，不阻塞施工。

### S1：领域模型与升级入口

依据：最终设计第 1—3、7 节。对应 A01、A12、A15。

- [x] 建立当前空间模型与集中语义定义，保留地点／路线身份；框架、空间边界、空间要素及必要关系落在 Map 领域。
- [x] 明确地点位置、真实占地、线路几何之间的引用和唯一所有者；清除重复几何存储方案。
- [x] 实现框架正逆／组合、引用与环检查、几何合法性、容量与数值校验；普通空间不依赖题材专用数据模型。
- [x] 更新空 Map、解析、序列化及领域编辑类型；集中引用格式版本和约束，不在多个消费者重复常量。
- [x] 实现受支持旧输入到当前模型的独立转换，验证旧坐标含义、未知映射和地点景观标签的保留。
- [x] 确认转换保存失败和未确认路径不会覆盖原始数据，不在 renderer 反复迁移。

关卡：冻结旧格式的结构与容量边界测试能经入口转换、保存和重读，保留事实及失败路径断言通过；非法输入不发布半个新模型。没有该关卡，不接用户实际存档做渲染试验。

### S2：关联编辑与安全删除

依据：最终设计第 6 节。对应 A07、A08、A11、A14。

- [x] 将现有地点关联组扩展为真实空间修改依赖，检查旧引用、请求后的引用及未编辑但受影响对象。
- [x] 区分整体移动框架与保持位置的换框架意图，由插件计算相关转换；以最终候选验证，不按父级先写的中间状态拒绝合法修正。
- [x] 将地图包含、所有权、坐标、承载和目的地关联分别处理，不因“共享同一地图”把所有独立项绑死。
- [x] 完成 owner 删除、目的地解关联、存活框架引用和承载引用检查，复用既有 staging／保存。
- [x] 对正序／逆序、非法必要前提、独立成功组、无变化和同轮修参检查实际数据及结构化回执。

关卡：关联失败不留半成品；跨区源对象与独立合法组不被误删或回滚。不能只测试编译返回成功而不检查保存后数据。

### S3：统一投影

依据：最终设计第 2—4 节。对应 A02、A05、A06、A10、A16。

- [x] 将浏览成员与空间可定位性归入领域纯投影；层级和到访继续使用现有唯一推导。
- [x] 分开输出全部地点、标记、线路、空间要素、未定位原因、源引用和全图范围。
- [x] 完成目标框架转换与地区范围裁剪；保持源几何可追溯，不将 UI 裁剪结果交给写入工具。
- [x] 移除旧示意排列和以直接父级相等决定最终坐标的运行分支；无定位内容保留列表入口。
- [x] 用海域、人工结构和非共面楼层输入验证投影不依赖默认陆地；路线连接不被当作道路几何。

关卡：同一地图事实只有一份浏览投影，世界／地区结果与真实框架一致，UI 不再另算成员和空间位置。

### S4：正常维护与管理员一起接入

依据：最终设计第 8 节。对应 A05、A11、A13。

- [x] 扩展现有 AtlasRead／AtlasEdit，围绕地图选择与空间内容组织高层意图；内部标识生成和几何计算留给插件。
- [x] 初始注入和分页读取共享内容投影，处理完整性、容量及可编辑源几何；提供当前已知地图／框架引用。
- [x] 普通维护与管理员复用内容 Schema 和业务说明，保留各自真实外层包装；清除旧 Atlas 描述覆盖分叉。
- [x] 按最终实现同步维护指导、字段说明、错误语义和可执行示例；不在人工文档复制一套未来 Prompt。
- [x] 样例覆盖最终设计 T01—T06，使用正式工具执行，再保存、重建 service 并读回；不得直接注入内部新类型跳过工具入口。
- [x] 检查现有供应商 Schema 转换，以及分页、省略／清除、同轮修参和增量保留。

关卡：两个消费者都能完成源事实读改回环；不能以正常维护通过替代管理员验证。模型只需组织空间，不被要求手工生成渲染细节或关联坐标副本。

### S5：通用空间绘制

依据：最终设计第 3、5 节。对应 A08—A10、A14、A15、T01—T06。

- [x] 从通用面积、边界、线带与点位开始，建立材质和语义配方；先验证无专属资产也能表达原始内容。
- [x] 实现环境／承载表面、覆盖／地势、结构／通道、真实标记的组合。优先使用现有 SVG 的 clip／mask 等能力，不为此直接引入外部几何服务或依赖。
- [x] 实现水上平台、桥面与其他已声明承载关系，拒绝无法由语义确定的冲突，不让模型负责手工裁切。
- [x] 加入稳定源空间细节：植被、山纹、建成区块面、人工表面、空间群集；有限枚举、稳定子集、跨图连续。
- [x] 城市背景与真实建筑占地采用不同交互语义；背景纹理不生成街道或地点。
- [x] 同一实现验证各题材，禁止按样例名、地区名或特定剧情写渲染分支。

关卡：不能只看单张精修图。所有题材的空间关系、材质辨识、边界和增量稳定性均需有真实生产组件结果。

### S6：三层界面集成

依据：最终设计第 4、5.3 节及总体设计第 3 节。对应 A03、A04、A06、A16。

- [x] 替换 MapApp／MapAtlas 的地点数量挂载条件，让空间要素决定绘图能力，地点数量只决定地点列表状态。
- [x] 接入统一全图范围，保留当前视口；未定位选择只打开真实详情，不假居中。
- [x] 世界／地区／Scene 入口保持既有含义，包含地区级定位、其他地区浏览及返回路径。
- [x] 在已有文案定义处添加必要未定位表达；UI 使用语义原因，不比较错误文本。
- [x] 验证窄屏地图高度、缩放按钮、详情和搜索，真实标记不会被细节或标签掩盖。
- [x] 检查 UI、装饰资源、切聊、卸载和后台运行状态，不新增模型触发点或持久化浏览状态。

关卡：有图无地点、有地点无坐标、完全空白、读取／保存故障都各自可理解，且不改变既有保存安全反馈。

### S7：完整验收与交付

- [x] 执行第 4 节自动检查及最终设计确定性行为／题材验收，记录命令、环境和证据路径；真实模型验收单列如下。
- [x] 在正式构建中复验生产组件，不只验收开发样稿；确认构建文件引用闭合、Atlas 不额外拉取三维世界资产。
- [ ] 真实模型验收在授权的独立测试聊天中执行，按第 5 节留证；无授权或未执行时明确保持待验收。
- [x] 自查上下游、错误路径、数据保留、源／派生信息边界、生命周期和真实用户体验。
- [x] 交付改动、原因、验证和未执行项；不自动提交／推送，不以测试全绿冒称真实模型质量通过。

关卡：规格范围没有缺口，证据可复核；如仍缺真实模型质量结果，只能报告实现与确定性检查的完成范围，不能宣布第一版全部验收完成。

## 4. 检查方式

### 自动检查

命令从仓库根目录执行，依据当前 [package.json](../../../package.json)。施工时先做相关测试，交付前再做全量；文档整理本身不触发生产构建或真实模型请求。

```powershell
npm run test:xiaobai-os
npx vue-tsc --noEmit -p tsconfig.xiaobai-os.json
npm run lint:xiaobai-os
npm run lint:imports
node scripts/check-garbled.js
npm run build:xiaobai-os
git diff --check
```

新增或修改的 JavaScript 测试另用现有 ESLint 检查对应文件；不自动修复无关文件。

- 新测试先说明保护的契约、真实失败边界、现有检查缺口和最低有效层。
- 逻辑与几何用单测，工具／转换／保存用集成，挂载与关键交互用真实组件浏览器；不跨层重复证明内部算法。
- 断言数据、位置、选择、事件、语义标识和保存结果，不断言人类文案或读取源码做存在清单。
- 禁止只验证返回码、不验证最终 Map；按最终设计第 7 节检查格式支持，并分别记录构造测试与真实存档补测结果，不能冒称已执行未提供的样本验证。

### 浏览器与视觉

复用 [生产组件验收环境](../../../output/map-production-check/README.md)，扩充工具输入 fixture 与验收入口，而非搭建第二套地图实现。

执行最终设计 A／T 项时记录桌面、390px、320px、深浅主题及实际浏览器版本；检查首次进入、增量更新、缩放／全图、返回、搜索、未定位、错误态和卸载。截图必须来自正式组件，触摸模拟与实体 WebView 结果分别标注。

除视觉核对外，记录浏览是否产生维护请求、事实是否变化和视口是否跳动。画面与测试结果共同判定，不能仅靠截图文件存在或自动断言全绿。

## 5. 真实模型验收

目标是证明工具分工让 AI 可以持续维护通用空间，不是证明它能输出合法 JSON。

- 覆盖最终设计 T01—T06；每类包含首建、保持已有内容的局部修改，以及涉及关联关系的后续调整。
- 保留自然语言输入、原始工具调用与回执、最终确认的 Map、截图及前后差异。不得手修坐标后称为模型结果。
- 分别检查空间合理性、题材表达、工具可用性、增量稳定性与数据安全；报告每类完成与失败，不只给一个总成功率。
- 正常维护与管理员至少分别完成一次涉及新空间内容的真实读改回环；不能只让一个消费者走模拟链路。
- 记录模型／供应商配置和调用成本信息，但不把密钥或敏感配置写入仓库。
- 使用获准的独立测试聊天和既有调用流程。未授权、不具备环境或未执行的项目保持待验收，不自动消耗模型额度或修改真实聊天。

## 6. 当前进度与证据记录

| 阶段 | 状态 | 证据 |
| --- | --- | --- |
| S0 基线与旧格式冻结 | 完成 | 独立 V1 类型、校验和转换入口；结构及近容量边界测试 |
| S1 领域与转换 | 完成 | `domains/map/space/`、`upgrades/`、Map 分区解析与当前格式写入 |
| S2 原子空间编辑 | 完成 | 最终候选依赖组、批量删除闭包、保存失败保留原数据 |
| S3 统一投影 | 完成 | 全部地点与可定位子集分离，明确框架与裁剪边界 |
| S4 两个工具消费者 | 完成确定性集成 | 共享内容契约，普通 Session staging／commit 与管理员分页读改回环 |
| S5 通用绘制 | 完成确定性检查 | 七类样例、源坐标装饰、水陆／承载遮罩与 SVG 像素检查 |
| S6 界面集成 | 完成浏览器检查 | 三种宽度、深浅主题、空地点地图、未定位和地区级定位 |
| S7 全量／浏览器／真实模型验收 | 确定性部分完成；真实模型未执行 | 下方实际执行记录 |

A／T 编号的确定性证据见下表；真实模型完成度另列，不能从构造样例推导模型质量。真实用户存档仍仅作为补充确认，不是支持范围或开工条件。

在本节记录日期、实际改动版本、验收编号、命令／浏览器环境、结果与证据路径；未执行、失败和不适用必须分开。长期规格不固化易失真的测试总数。

### 2026-09-26：S0 基线与样本调查

环境为 Windows／PowerShell；检查对象为 `1b43f9a9cdbbb5675ddb7f0cf41fc38a303fd65c` 上的当前 dirty worktree，不是该提交的干净版本。这条历史记录当时只涉及施工检查，尚未修改运行代码或构建产物；后续实现在下方另记。

- 可改范围为 Map 领域、Map APP、对应测试与文档；已有地图层级修复保留。`scripts/gold-eval/` 和 `scripts/story-summary-replay/` 的用户修改不在范围内。
- 已核对磁盘读取链：`storage/sillytavern-file-storage.ts` → Kernel `snapshotFromEnvelope`／分区注册解析 → `apps/map/partition.ts`；维护与管理员均经各自 session 调用共享 compiler，再经 `MapService.replaceCurrent` 和既有事务保存。
- 分区 `parse` 与 `serialize` 当前均直接调用 `parseMapDomain`；`domains/map/state.ts` 的根对象读取、领域 edit 和主 RP projection 另有当前模型校验。新旧格式转换尚未写入任何入口，也未发现独立的 Map 文件导入器。

基线检查（不是 A／T 新功能验收）：

| 命令 | 结果 |
| --- | --- |
| `npm run test:xiaobai-os` | 通过，1488 passed／0 failed |
| `npx vue-tsc --noEmit -p tsconfig.xiaobai-os.json` | 通过 |
| `npm run lint:xiaobai-os` | 通过 |
| `npm run lint:imports` | 通过 |
| `node scripts/check-garbled.js` | 通过 |
| `git diff --check` | 通过 |
| `npm run build:xiaobai-os` | 未执行；尚无运行代码改动，不重写已有构建产物 |

样本调查只读文件，没有修改聊天或发起模型调用：

- 当前 SillyTavern `config.yaml` 的 `dataRoot` 为 `./data`；该目录含隐藏／忽略文件的搜索未找到 `LittleWhiteBox_OS_*.json`。
- 工作区 `output` 中找到并检查了 32 份非索引 OS sidecar，仅一份包含 `map`。其来源可追溯至 `output/playwright/roll-native-gate/interop.cli.js` 中直接构造的 `interop-hall` 单房间 fixture；没有位置坐标或 Scene，不是实际用户地图，不计作 A12 样本。
- [生产组件验收环境](../../../output/map-production-check/README.md)及 `tests/fixtures/map-atlas.js` 也明确使用隔离工具输入／构造数据，不能替代真实历史样本。

**关卡修正：** 用户明确真实存档仅用于补充确认。先前因缺少真实样本暂停施工的条件撤销；支持范围与实现责任统一按最终设计第 7 节执行。下一步依现行代码冻结旧格式、建立结构与边界测试，再实施 S1，不等待用户交样本。此前调查记录仅说明尚未执行真实存档补测，不表示格式不受支持，也不算 A12 已通过。


### 2026-09-26：实现与确定性验收

实现仍在 Map 原分区内。当前模型为 `MapDomain`；旧输入只经 `upgrades/read.ts` 转换。框架不借用父子包含关系，缺失映射保留原坐标并显示未定位；没有迁移到第二套状态。纯浏览、装饰、缩放与切层不发维护请求。

| 编号 | 本次证据与结果 |
| --- | --- |
| A01、A02、A05 | `map-space.test.js`／`map-browse.test.js`：正逆映射、未知局部框架、列表成员、无端点路线、单向箭头与源几何分别验证 |
| A03、A04、A06 | `check-atlas.cjs`：纯地形世界／地区、未定位详情及 Scene 入口、不假聚焦、地区级玩家、远处地形全图与普通更新保留视口 |
| A07、A11 | `map-space.test.js`：跨区河流删除保留、关联目的地删除、支持面依赖、同次关联编辑回滚、无映射关联删除不依赖输入顺序 |
| A08、A14 | `check-atlas-composition.cjs`：583 个河面像素检查；甲板、植被、嵌套承载边界；单测拒绝循环、非法轮廓及矛盾表面 |
| A09、A10 | 源装饰 ID／坐标／尺寸在不同 LOD 保持一致；真实组件世界／地区共享可见源符号。城市屋顶是只读纹理，水域、道路、公园与真实占地参与遮罩，不增地点 |
| A12 | 独立冻结 V1 解析器；构造覆盖旧结构、512 个地点及近 512 KiB 输入，原坐标、Scene、到访与人物不丢；失败保存不改原 V1 文件，重读幂等。**未使用真实用户存档** |
| A13 | 七类工具输入均经编译、保存、重新打开及管理员完整分页；普通维护实际 Session 读改暂存／提交，以及重绘未解决错误禁止保存 |
| A15 | 细节枚举和输出均受限；256 要素全部显示，无截断，卸载移除 SVG。最终遮罩合并后桌面一次更新实测约 98 ms／3104 SVG 元素；不是实体手机帧率，也不是所有几何组合的最坏耗时 |
| A16 | 全量现有回归，以及三层浏览检查；主 RP 不加入地形几何、Scene 三维模型与保存协议未改 |
| T01—T06，另加沙漠 | 七类可执行工具样例经同一生产组件；1280／390／320px × 深浅主题共 42 组，通过世界／地区范围、无坐标入口、浏览不改事实／零 Host 请求检查 |

浏览器：Windows Chromium **153.0.8010.52**。原生触摸事件模拟、键盘打开／关闭及焦点返回、125% 页面缩放和卸载／重挂均有执行；未测试实体 WebView。截图来自正式组件，不是另画的设计稿，位于 `output/playwright/atlas-*.png`。像素检查直接栅格化组件生成的 SVG，未手修图面。

执行入口：
- `node --import tsx output/map-production-check/build.mjs`
- `node output/map-production-check/serve.mjs`（隔离、只读、仅监听 127.0.0.1）
- Playwright CLI：`run-code --filename output/map-production-check/check-atlas.cjs`、`check-atlas-composition.cjs`、`check-regions.cjs`
- `node output/map-production-check/check-2d-build.mjs`：真实 Atlas／Scene 二维构建禁止 Three，通过。

检查记录：本次全量单测 **1515 passed／0 failed**；随后定点空间／层级／管理员回归通过。类型检查、OS lint、修改的 JS 测试 lint、相对导入、乱码检查、正式三出口构建、`git diff --check` 通过。构建替换的是可再生成的哈希 chunk，不是用户地图文件；没有新增运行依赖。构建仅有耗时／预览 Three chunk 体积提示，不影响通过。

三层原始问题复验：`check-regions.cjs` 的三种宽度 × 深浅主题以及生命周期、同名地区 key、长名称／即时状态更新均通过。世界横幅为 3 地区／2 未到访时只列那 2 个地区；当前地区为 10 场景／5 未到访时只列那 5 个场景。

**未执行／未宣称：** T01—T06 真实模型首建与连续维护、真实用户存档补测、实体手机／WebView 验收。未调用模型、修改真实地图、提交或推送。真实模型质量未测，不把工具集成成功或截图当作 AI 质量证明。

### 2026-09-26：连续地图与地貌材质的视觉修正

用户指出此前世界／地区被压进方框，材质表达也不足。本轮按最终设计第 5 节修正 Atlas 表现层；上方首轮验收的 98 ms、1515 项记录保留为历史结果，不代表本轮材质成本或最终检查数量。

实施范围：

- `MapApp`／`MapAtlas`／`MapViewport` 和 `map.css`：地图铺满 APP，控件悬浮；打开是地理视野，明确按“全图”才完整收纳已知范围。`atlas/camera.ts` 与 `use-insets.ts` 统一处理比例、控件避让和详情聚焦；无坐标地点仍不假定位，Scene 保留原视口行为。
- `atlas/AtlasBackdrop.vue`：连续底图，未知覆盖只用中性点纹，不补造陆地、海洋或星群。必要交互文字继续归 `map-copy.ts`。
- `atlas/noise.ts`／`surface.ts`／`texture-sheet.ts` 与 `AtlasSurface.vue`：源坐标确定生成连续地貌材质，并合并为一张运行期纹理；山脉明暗、森林冠层、沙丘、城市屋顶、水域、人工表面和太空材质不再依赖重复小图标。原空间轮廓与水陆／承载遮罩保持权威，纹理不生成地点或道路事实。
- 纹理随 Atlas 组件释放，不存地图、不增加全局缓存。平移、缩放及仅改名称／人物不重编码；采样预算降分辨率而不删除要素。没有新增运行依赖、存储字段、工具契约、模型触发点或真实用户数据修改。
- 新增 `map-atlas-art.test.js` 保护开图／全图、聚焦避让、源材质确定性和采样预算；浏览器复用生产组件，新增 `atlas-geography.mjs`、`check-atlas-art.cjs`、`inspect-atlas-art.cjs`。组合输入只用于验收，不是模型生成或用户存档。

实际验证环境仍为 Windows Chromium **153.0.8010.52**：

| 检查 | 本轮结果 |
| --- | --- |
| 全量 OS 测试 | **1519 passed／0 failed**；后续纹理合并由下列最终构建及浏览器检查复验 |
| 类型、OS lint、修改的 UI／测试 JS lint、相对导入、乱码、正式三出口构建 | 通过；最终纹理合并及采样间隔已包含在类型检查与构建中 |
| `check-atlas.cjs` | 最终材质版本七类 × 三宽度 × 深浅主题，共 42 组通过；另覆盖纯地形、未定位、地区级定位、全图、源材质跨层稳定、触摸、键盘、125% 缩放及卸载 |
| `check-atlas-art.cjs` | 最终版本六组组合地图通过连续画布、开图／全图、定位避让、只读浏览；短横屏控件无重叠，缩放／全图／改名材质编码新增次数为零 |
| `check-atlas-composition.cjs` | 最终版本 583 个河面像素及甲板／嵌套承载通过；256 要素全部绘出，2049 SVG 元素，一次桌面更新约 **633 ms**，卸载移除绘制内容。此值包含材质生成，不是实体手机帧率或所有地图的最坏耗时 |
| 三层浏览与二维边界 | `check-regions.cjs` 通过既有范围／定位／Scene 回归；`check-2d-build.mjs` 通过 Atlas／Scene 二维入口禁止 Three 检查 |

正式组件实图位于 `output/playwright/atlas-geography-*.png`（桌面／手机、开图／全图、世界／地区、短横屏）；七类材质图仍为 `output/playwright/atlas-*.png`。已实际查看深浅主题及代表性内容，不以截图生成或自动检查冒称用户视觉认可。

**边界：** 本轮改变的是布局、相机及程序材质，不是三维世界实现；未执行真实模型调用、用户存档补测、实体手机／WebView 验收，未提交／推送，也未触碰并行的故事总结工作。

### 2026-09-26：七项 review 问题修复

本轮只修复已确认的七项缺陷，不改存档格式、维护／管理员工具契约或 Scene 三维实现，不增加依赖、模型请求或持久状态。

| 问题 | 修复与定点证据 |
| --- | --- |
| 长详情遮住聚焦标记 | 选择地点后，在 DOM 更新完成时同步测量遮挡，再发出聚焦动作；普通内容更新仍不重置相机。390px 宽、844／600px 高浏览器复现均通过，标记与上下控件分别留有约 122／57px 的距离 |
| 图集高度超出浏览器上限 | 高度优先排列；同时限制图集宽高，空间不足时先扩宽再调整采样密度，不丢要素；编码失败显式抛出。原 256 个横竖长条从 1024×98560 修正为 1024×1282，实际 PNG 解码通过 |
| 两像素窄地貌全透明 | 将外部距离场放到纹理之外的边界采样中，不占用实际地貌首尾像素；森林和山脊在两个方向的窄条及极小局部坐标下仍保留可见像素 |
| 全图后缩小反而放大 | 缩放限制使用同一全图相机尺度；视口超出更新后的限制时也不反转操作方向。100×10000 地形浏览器缩放往返通过 |
| 地形联动修改依赖顺序 | 领域统一计算终态冲突与旧／新占用关系，各版本使用自身框架映射；原子组从冲突涉及的地形追溯到被修改的依赖。移位、交换、删除让位、仅移动框架均不依赖输入顺序，非法联动一起回滚且独立项仍可成功 |
| 共用斜边被视为面积重叠 | 独立面积相交判定使用实际轮廓，包围盒仅做排除；覆盖反向轮廓、凹形接边、完全重合、包含和真实相交。两个共享对角线的异材质三角形正式工具提交成功 |
| 同材质去重擦掉跨河桥面 | 去重同时比较跨越关系的集合语义。两个排序相反的桥 ID 均保留石质桥面像素，河流其余位置仍为水面 |

职责落点：领域新增 `space/area-overlap.ts` 与 `surface-conflicts.ts`，由领域校验与工具关联分组共同消费；UI 修复保留在既有 Atlas 相机、材质及组合模块。没有把这些规则复制到 Prompt 或两个工具消费者。

新增五项纯逻辑／工具回归，位于 `map-space.test.js`、`map-atlas-art.test.js`；实际绘制与定位回归位于 `output/map-production-check/check-atlas-regressions.cjs`。全量测试 **1524 passed／0 failed**，类型检查、OS lint（本次新增的注释格式警告已修正并定点复验）、修改 JS lint、导入、乱码、正式三出口构建和 `git diff --check` 通过。七类地图 × 三宽度 × 深浅主题的 42 组浏览器检查通过。实图为 `output/playwright/atlas-fixed-*.png`；数据均为隔离构造输入，不是用户存档或模型输出。

补充复验：`check-regions.cjs` 的六组主题／尺寸与生命周期检查、`check-atlas-art.cjs` 的六组连续地图及纹理复用检查、二维入口禁止 Three 检查均通过。`check-atlas-composition.cjs` 的 583 个河面像素与嵌套承载仍通过；256 要素一次桌面更新实测约 707 ms、2049 SVG 元素，不将其当作实体手机帧率。

**相邻问题单列，未扩大本轮范围：** 390×600 等较矮窗口打开长详情时，右侧缩放控件和定位／图例按钮仍可能重叠；这与聚焦标记是否可见是不同问题，见 `atlas-fixed-focus-600.png`。本轮没有调整它们的布局，也不宣称所有窗口组合已无体验问题。

### 2026-09-26：关闭矮窗口控件重叠问题

接续上条相邻问题，只调整 Atlas 操作布局与屏幕遮挡测量：

- `MapApp` 统一拥有缩放、全图、定位、图例的悬浮操作栏，与详情参与同一布局；不再让两组控件各自绝对定位。按钮保留 42px 操作尺度，详情正文在自身区域滚动，透明留白仍可拖动地图。
- 根据 APP 容器而非浏览器高度响应：宽屏横幅与操作栏同排；宽而矮时详情侧置，顶部导航留在地图一侧。`use-insets.ts` 根据实测面板边界派生底部或右侧遮挡，不复制布局断点，不改变地图坐标或普通更新时的相机状态。
- `MapZoomControls` 是 Atlas 和 Scene 二维缩放按钮的唯一实现；相机仍由既有 `MapViewport` 持有。Atlas 通过当前相机实例执行操作，不新增第二份视口状态。Scene 二维默认布局及三维实现未改。

验证：

- 新增 `output/map-production-check/check-atlas-controls.cjs`：320×600、390×600、390×844、600×390、844×390，以及大浏览器中嵌入 844×347 APP 窗口，深浅主题共 12 组。检查五个按钮中心真实命中、操作尺寸、鼠标／键盘缩放、图例开合、回到当前位置、关闭详情和切层；长详情的定位标记可见，正文保留可滚动空间。另检查 125% 缩放、原生触摸点击和错误提示。
- 触摸验收初次误把“已到放大上限、不再变化”当成点击失败；鼠标对照确认同样受限。修正测试为从全图开始并等待实际视口变化，原生触摸缩放通过；未修改生产缩放限制迎合测试。
- 全量 OS 测试 **1524 passed／0 failed**；类型检查、OS lint、导入、乱码、正式三出口构建、二维入口禁止 Three 和 `git diff --check` 通过。`check-regions.cjs` 六组主题／尺寸及生命周期、地区级定位回归通过。
- 最终串行复验 `check-atlas-controls.cjs` 全部通过，包括 12 组布局、125% 缩放、原生触摸和运行中转失败提示；`check-atlas.cjs` 的 42 组题材／主题／宽度及纯地形、未知定位、生命周期通过；`check-atlas-art.cjs` 六组连续画布、全图、定位及纹理复用通过。浏览器为 Windows Chromium **153.0.8010.52**。
- 实际查看窄屏深浅主题、600／844px 矮横屏的生产组件截图；新证据位于 `output/playwright/atlas-controls-*.png`。未把检查完成或截图生成当作用户对视觉的认可。

边界：地图仍连续铺满 APP。未改用户存档、地形事实、工具契约、Scene 三维实现；未新增依赖、持久状态或模型调用，未提交／推送。浏览器验收使用隔离内存 fixture，未测试实体手机／WebView。

### 2026-09-26：Atlas 示例归入共享工具说明

- 将现有 `atlasExamplesPrompt()` 的唯一注入点移至共享 `MapAtlasEdit` description，删除后台维护固定提示中的原注入。后台维护与管理员沿用各自保存说明，共用同一份示例；领域概念和 Scene 示例保持原位。
- 示例数据仍只定义在 `atlas-examples.ts`，没有管理员版、精简版或测试副本。模型收到的是现有沙漠示例，七类题材继续作为不同的可执行测试输入，不把七份全部注入。
- 管理员沿用现有 `ToolsLoad`：地图工具未加载时不携带示例，加载后随工具定义进入本次任务后续请求。没有新增查询工具、缓存、配置或持久状态。
- `administrator-tool-loading.test.js` 新增一项回归：从实际发布说明提取调用数据，验证两边示例相同、固定提示没有重复示例，并通过真实维护／管理员执行链确认后台暂存后提交、管理员调用即保存，最终地图一致。测试不固定周围说明文案，示例输入不另抄一份；修改前此测试因工具缺少示例失败，修改后通过。
- 当前工作区全量 OS 测试 **1532 passed／0 failed**，包含七类 Atlas 集成回归；最终示例解析调整后又定点复验通过。类型检查、OS lint、修改测试 lint、相对导入、乱码、正式三出口构建及 `git diff --check` 通过。构建仅有插件耗时提示。

边界：本次只调整示例归属，未改地图格式、绘制或 Scene，未改管理员加载机制，未新增依赖、修改用户存档、调用真实模型或提交／推送。未重跑浏览器视觉验收；这些结果证明加载／执行契约，不代表真实模型的绘图质量测评。
