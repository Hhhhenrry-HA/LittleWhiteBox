# 钱包 APP

钱包是 Economy 的只读用户界面。它展示当前聊天的小白币余额、流水分页和保存/剧情对账状态，不拥有余额写入、调账、任务、商店、银行、游戏或宠物规则。

`host/controller.ts`只把 Economy 投影为可序列化 DTO，并处理激活、刷新、分页和未确认保存的核实；`ui/`只负责显示与交互。钱包没有`post`、`adjust`或`set-balance`命令。

删除钱包只需删除本目录以及 host/shell 的两条注册。若仍有其他真实消费者，必须保留 Economy 及其流水；删除钱包不等于清空账本。
